{
  "name": "@coin-cast/core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --format esm --sourcemap --clean --out-dir dist"
  }
}
